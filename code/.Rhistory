# This script runs the Data cleaning, summarizing, visualization, and estimation scripts. The rmd used to generate the output needs to be executed separately.
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# Defining globals
input_dir = "input"
code_dir = "code"
output_dir = "output"
source("code/setup.R")
source("code/estimations.R")
source("code/visualization.R")
source("code/iterated_regs.R")
# This script runs the Data cleaning, summarizing, visualization, and estimation scripts. The rmd used to generate the output needs to be executed separately.
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# Defining globals
input_dir = "input"
code_dir = "code"
output_dir = "output"
source("setup.R")
source("estimations.R")
source("visualization.R")
source("iterated_regs.R")
